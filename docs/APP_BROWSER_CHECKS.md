# Application journey verification — candidate 003

Actual parent-operated browser checks, 12 September 2026. Main app: port 3210, original user tab 12 at 985 × 998. Isolated QA: port 3214, 1280 × 720 and 390 × 844. QA has no keys, no .env files, and an outbound-fetch guard. Only its copied store received test writes. No paid API calls or external calendar/message/purchase actions occurred in this correction.

## Observed journeys

- Overview, directory, mentor detail, studio, Council, Review, tools, Sources, Shared context, Profile and Your day have distinct browser URLs and focused content in one persistent app shell. Direct editor/review loads work. Unknown `/mentoros/not_a_page` renders a recovery page; Go to Overview, browser Back and Forward restore the appropriate pages and heading focus. The server suite covers all named deep links, unknown/encoded paths, HEAD, missing assets and APIs.
- The main dashboard presents four actual saved mentors, one pending council recommendation and the user's separately approved six-event synthetic day. Pending versus approved previews have different status/actions/copy. There are no invented completion percentages or progress maps.
- Directory → Mira → Edit opens a full-page editor. A draft survives leaving and browser Back. Profile Cancel did not erase a separate mentor draft. Explicit mentor Cancel discarded its draft; existing-mentor Cancel returns to its detail page. A manual edit to Mira QA saved, routed to detail and survived reload. A new manual Quinn reading mentor saved to its own profile. These writes were only in QA; their pre-reset store is retained as `ecosystem-browser-crud-backup.json`.
- Independent current-source deferred-mutation probes cover repeated Create saves, away/back while saving, newer edits, identity binding, Cancel/delete and saving mentor A while editing mentor B; see APP_CODE_REVIEW.md. These are isolated logic probes, not network-delay browser simulations.
- Main Council shows three selected mentors and the actual saved 17-message conversation, with from/to, proposal/tool/peer-review/resolution labels and an existing generated image. Full-message/question/analysis disclosures are available. Its editable prompt and voice controls are on Council; starting another council is correctly gated while the existing recommendation needs a decision. No new AI run was submitted.
- Review is a separate page. Main remains pending. The three real saved alternatives expand with costs/unknown prices and caveats. The saved run reports no unresolved conflicts; it is not represented as live disagreement evidence.
- QA keyboard approval: expanded full summary, submitted Approve with Enter, observed `Approved by you` as the focused `role=status` element and the summary still expanded. The first attempt exposed focus returning to body when disabling the button; the final source fix passed the repeated browser check. No main recommendation was approved during this pass.
- Phone menu opens with readable labels, navigation closes it and focuses the destination heading. Studio inputs stack without horizontal overflow (390px viewport and 390px document width). The unavailable AI assistant now gives concise status and puts manual Name/Domain fields in the initial viewport. A typed draft was cancelled without saving.
- Tools hub navigates to separate meal, receipt and source workspaces. Missing-key controls are disabled with explicit local status. Typed meal content survived leaving for Tools and returning. Source page shows five actual council links plus a separate planner source, with concise expandable excerpts. Source page also measured 390px document width at 390px viewport. The existing generated image is visible in Council and Review.
- Shared context shows actual configured preferences/budget and approved memory, now expandable. Edit shared profile opens a populated full page; Cancel returns to context. Your day retains the approved snapshot, six commitments, prior coaching conversation, portraits and export control. No planner reset or external calendar action was performed.
- No duplicate DOM IDs were observed after approval. Main browser error log was empty. Route changes cancel pending recordings/transcription in source; physical microphone capture remains unverified.

## Evidence and limits

Actual screenshot files are under `.vision-loop/runs/two-projects-20260912/artifacts/renders/app-*`. Main filenames contain main; QA filenames contain qa. Superseded v1 screenshots are historical; current mobile studio/approved overview and sources use v2. Main Context uses v2. Approval keyboard result uses v2. Main Council uses v2 plus composer/alternatives captures. Quinn and Mira QA are manual test fixtures, not user mentors.

Automated validation: 194 tests, 193 passed, one optional live test skipped, zero failures. Type check, build and all frontend JS syntax checks passed. Log: `artifacts/app-structure-tests-final.log`. Main ecosystem and planner files matched their pre-correction SHA-256 baselines (`artifacts/app-runtime-preservation.json`).

This verifies the app organization and retained local workflows. New provider success is not claimed: OpenAI previously returned `credit_balance_exhausted` / `insufficient_quota`. Saved historical successful API results remain visible. Physical microphone, fresh AI drafting/voice/search/images, new uploaded-image analysis and a newly generated meal-to-council attachment were not rerun. The earlier ecosystem evidence documents those integrations; their current live readiness remains blocked by funding. Drafts survive in-app route changes, not browser reloads unless saved.

Progress/goals/check-ins remain intentionally paused in `.verification/paused-progress/`; no progress API or UI was integrated. Final acceptance of the revised layout belongs to the user.

A concurrent change added an Our vision route while this review was closing. Its code is preserved, but that page is outside the scoped screenshots and acceptance in this correction. Final regression checks include the current shared source; no claim of an independently reviewed Our vision experience is made.

## Funded provider recheck — 12 September, 16:11 Dubai

The actual routed meal page generated a GPT Image 2 high-quality 1024×1024 meal through its button, returned HTTP 200 after 137 seconds and rendered the returned image with no console errors. Network evidence includes the provider request ID in `artifacts/api-audit-20260912/funded-image-check.json`. The first attempt reached the old 90-second deadline; generation now allows 180 seconds. Both regenerated coach portraits loaded at 1024×1024 in the planner with no console errors. Temporary browser tab 27 was closed; user tab 12 was refreshed to Overview. The existing pending council and approved six-event plan remain unchanged. A separate real image council completed in 196 seconds, with eleven messages and five sources; evidence is in `funded-image-council-check.json`.
