# Progress implementation contract — 12 September 2026

**PAUSED by user:** first rebuild the existing MentorOS features as proper app pages. Draft code is preserved under `.verification/paused-progress/`; no progress routes or UI are integrated. Resume only after the app architecture correction.

Goal → approved commitment → mentee check-in → mentor response → next recommendations. Preserve current layouts, fonts, existing saved councils and approved planner. No made-up progress, completions or AI replies. Initial goals/actions empty except actual locally approved sources explicitly labelled (original planner is a synthetic demonstration).

Types in src/progress-types.ts are the shared contract. Parent owns this file, server routes, ecosystem/council/planner integration and AI responder. Backend worker owns src/progress.ts and test/progress.test.ts. UI worker owns only web/mentoros/progress.js and progress.css. Native fetch only. OpenAI project was last observed credit-exhausted; do not make live provider calls.

## Service API (backend worker)

ProgressService constructor options {statePath?:string; mentors:()=>CustomMentor[]; now?:()=>Date; reply?:(input:CoachReviewInput)=>Promise<CoachReplyDraft>; profileContext?:()=>string}.
Methods getState():ProgressState; saveGoal(input:unknown):ProgressState; saveAction(input:unknown):ProgressState; checkIn(input:unknown):ProgressState; addFeedback(input:unknown):ProgressState; async requestReply(actionId:string,mentorId:string):Promise<ProgressState>; importApproved(input:ApprovedActionInput[]):ProgressState; context():string.

Goal input: {id?,title,why,successCriteria,unit,baseline,target,targetDate:string|null,mentorIds,status:'active'|'paused'}. current initialized baseline, preserved on edits; current changes ONLY through check-in goalValue (goal-linked action). Target != baseline, all finite bounded numeric, dates real YYYY-MM-DD. Changing baseline/target allowed and preserves observations. At most12goals,200actions, checkins/feedback/replies bounded to500each, atomic0600 persistence, validate persisted file without destroying invalid data. Return cloned derived stats. Baseline→target numeric progress clamped0..100 supports increase/decrease. Metrics completion completed/ALL actions (skipped included) with null percentage for noactions. Overdue=noncompleted/nonskipped scheduledEnd or scheduledStart before now. Permentor metrics identify mentorId; shared actions countonceoverall. No implied real-world verification: checkins selfreported.

Action input: {id?,title,details,mentorIds,goalId:string|null,scheduledStart:string|null,scheduledEnd:string|null,checkInQuestion}. Initialmanual source generatedserver. No client-supplied source/status. Assignedmentors mustexist, >=1. Date time strings requiretimezoneoffset/Z; end>start; allowbothnull. Updates preserve source/status. Reject goals notknown. Checkin {actionId,status,note,blocker,goalValue:number|null}; status planned/in_progress/completed/skipped; note min1; goalValue only withlinkedgoal. Save event + actionstatus + goalcurrent atomically. Corrections via latercheckin. Checkin source is userreported, never calendar attendanceverified.

Feedback input {mentorId,actionId:string|null,suggestionId:string|null,suggestionText,category,point,requestedChange}; onlyexistingmentor, if actionId mustexistandmentorassigned; suggestion text max3000, referenceeitheraction/suggestion/general. Parent resolves suggestion IDs/text against current actualcouncil server-side before callingaddFeedback. UI can'tinventhistoricalsource. Feedback never silentlymutatesmentorinstructions; supplied to futurecouncils andresponses.

importApproved trustedserver-only: source.kind calendar/council, unique source.kind+id. Upsert title/details/schedule on existing source while preserving status, goal linkage, checkins, user question. Skip unchanged for idempotency/revisionstability. Newactions planned, defaultcheckinquestion specifictotitle. Existingremovedmentor IDs allowhistoricalimport? Preferfilterparent toknownmentors. requestReply uses latestcheckin, assignedactive mentor, linkedgoal, recentmentorfeedback/profile; dedupe mentor+checkin; blocksameconcurrentrequest; don'tpersistfakefallback onfailure; ensurecheckinhasn'tchangedbeforestoringresponse. Mentor can'tmarkactionscompleted ormodifygoalvalues. context serializesactivegoals, recentactions/checkins/feedback/replies bounded to~18000chars withclearselfreportedlabels.

## HTTP (parent integration)
GET /api/progress/state -> ProgressState plus mentors:CustomMentor[], suggestions:[{id,mentorId,title,text}], canReply:boolean configurationonly. SuggestionIDs current council messageIDs or decision:<runid>, the latter mentorId null (choosementor).
POST /api/progress/goals {goal} -> enrichedstate
POST /api/progress/actions {action} -> enrichedstate
POST /api/progress/check-ins {checkIn} -> enrichedstate
POST /api/progress/feedback {feedback} -> enrichedstate
POST /api/progress/reply {actionId,mentorId} -> enrichedstate
GET /api/progress/calendar.ics -> scheduled non-skippedactions as calendarfile (noexternalcalendarwrites)
Allmutations use existinghost/origin/jsonvalidation. Progresschanges invalidate pendingcouncil context. No progresseditsduringactivecouncil. Existingapprovalpaths importactualapprovedactions, notmerelypendingproposals; repeatedapprovalsno duplicates. Startup importsactualapprovedplanner'shealth/career nonfixedevents and currentapprovedcouncil. Councilcontext and planner get_day_context receive latestlongtermgoals/checkins/feedback. Not a newscheduler/notificationservice.

## UI (worker)
Parent inserts hidden section id=progress-view, loading empty content initially, CSS and deferredscript, adds Progress link/button. Worker selfrendersroot and exposes window.MentorProgress={show(),hide(),refresh(),feedback({mentorId?,suggestionId?,suggestionText?,actionId?})}. show unhides andloadsstate; hide hides andclosesdialogs; parenthides othermains. Worker can dispatch custom event mentor-progress-open on externalactions if needed. Parent calls feedback afterswitchingtab onexistingcouncil feedback buttons. UI may listen mentor-progress-refresh afterapproval. Defaults localdatetime handled conversiontoISO forserver. UTC+4existingapp but browserdateinputs useexplicit browserlocaltimezone label.

Build polished responsive sections: overview completion counts + goal outcomes, permentorprogress, longtermgoals CRUD/paused, actionablecommitments withschedule/goal/mentor andcheckin due status, reportupdateform, actualcheckinhistory, mentee feedback on specificsuggestions andmentor response with realfailure states. AI reply request explicit, retainuserreport iffails. Goal create/edit, action create/edit, checkin andfeedback can use accessible native dialogs. Progress means userreportedcompletion, notmentorqualityrank. Include permentor filteredactions and feedback access. Goalstargetdates and successcriteriavisible. Nofake charts or samplecompletion. Empty states explain nextstep. Approvedactions autoimport so user'sexistingday canbetracked; no hardcoded newusergoals. Data-driven completeflow offline exceptmentorAIresponse. Nativefetch, escapedHTML, accessiblelabels/focusrestore,390phone. Avoid pollingoverwritingopenforms; pollorrefresh onlyclosedview. Canvasgraphs notneeded; progressbars with values and accessiblelabels.
