# BRIEF independent review — mentor-ecosystem-002

**Verdict: FAIL for current live demo readiness — external provider funding blocker. Historical functional evidence: PASS.** The latest council speech attempt returned HTTP 429; inspected `artifacts/ecosystem-voice-recheck.json` confirms `credit_balance_exhausted` / `insufficient_quota`, consistent with the latest `ECOSYSTEM_VERIFICATION.md` addendum. This establishes exhausted provider credits, not a backend implementation failure. The earlier successful runs remain valid historical evidence from minutes before the blocker; they do not establish current live availability. Funding confirmation remains pending. No current live voice success is claimed.

New OpenAI-dependent council, AI-drafting, image/receipt and voice operations are blocked pending restored funding or a funded key. The request to add credits or supply a path to a funded key remains pending; no resolution is assumed. No further paid calls are authorized for this review. Existing saved results, generated-image viewing and manual mentor editing remain usable. This does not establish an independent Exa outage.

The capability labels in `web/mentoros/ecosystem.js` now say **configured / not configured**. They indicate configuration only, not remaining credit or service health; older captures showing “available” are historical. No blocking missing implementation was identified, but the provider-dependent live demo cannot currently be represented as ready.

Reviewed `ECOSYSTEM_TARGET.md`, `ECOSYSTEM_VERIFICATION.md` including its latest focus/funding addendum, `UI_INTERACTION_EVIDENCE.md`, `VOICE_VERIFICATION.md`, live artifacts including the saved quota diagnostic, saved state, implementation and relevant test source. I inspected actual captures using `view_image`; I did not operate or test the UI, invoke providers, reproduce the quota diagnostic, rerun tests, or mutate application state. Only this report was written.

## Historical journey and outcome evidence

- **Any-domain mentor creation and persistence:** the builder capture shows an AI-generated, editable vacation mentor with instructions, goals and tool permissions. `artifacts/ecosystem-builder-and-gym-state.json`, `artifacts/ecosystem-three-mentor-live.json` and current `.mentor/ecosystem.json` contain the same custom Roam identity. Four mentors persist, and Roam actually participates. `src/ecosystem.ts` implements arbitrary domain fields, AI drafting, editing and disk persistence. This is a bounded mentor builder: 12 saved mentors, 2–4 council participants, three supported tool permissions and two voice presets.
- **Real communication, meaningful coordination:** the three-mentor artifact contains 17 messages, six directed peer reviews, five retrieved sources and one generated-image result. Roam explicitly replaces its sandwich fallback with Mira's receipt-based bowl and accepts the shorter walk. Penny protects savings and connects outing costs to optional gym visits. `src/council.ts` creates a distinct hosted session per mentor, supplies actual earlier proposals to each review, and synthesizes the resulting positions. Separate roles are not merely dialogue produced by one synthesis call. Provider session IDs are not retained in these saved UI artifacts; session separation is corroborated by implementation and test source.
- **Shared budget and approval memory:** `artifacts/ecosystem-gym-live-v2.json` computes AED 250 from `min(250, max(0, 12000−9500−2000))` and recommends home-first fitness. The subsequent three-mentor artifact and current saved state contain exactly one approval memory dated 10:24:02.199 UTC; the newer recommendation remains pending. `src/ecosystem.ts` enforces current-run approval, idempotency and context invalidation; rejection adds no memory. Relevant coverage is in `test/ecosystem.test.ts`.
- **Tools serve the outcome:** `artifacts/ecosystem-vision-live.json` records HTTP 200 and receipt values 8 + 12 + 6 = AED 26. The council attachment carries those ingredients without asserting real spending or ownership. `artifacts/ecosystem-meal-live.json` records successful image generation; the three-mentor run also contains its own image URL. Roam's actual Exa results identify local grocery candidates while preserving unknown prices/access. `src/mentor-media.ts` and `src/providers.ts` contain the provider calls; `web/mentoros/ecosystem.js` renders known image references without executing arbitrary Markdown.

## Visual evidence and remaining boundaries

Inspected under `.vision-loop/runs/two-projects-20260912/artifacts/renders/`: `ecosystem-desktop-v2.jpg`, `ecosystem-phone-v2.jpg`, `ecosystem-decision-desktop-v3.jpg`, `ecosystem-decision-phone-v2.jpg`, and `ecosystem-builder-desktop-v1.jpg`. The paired captures show coherent vivid accents, distinctive typography, readable mentor identities and a visible approval action on desktop and phone. `ecosystem-meal-ui-v1.jpg` and `ecosystem-receipt-ui-v1.jpg` support completed tool states only; their geometry is historical.

The inspected `ecosystem-builder-phone-v2.jpg` shows the builder reflowed into a single column, with the assistant description, Draft with AI control and editable name/domain fields visibly contained within the phone dialog. This is layout evidence only; it does not establish successful AI drafting after quota exhaustion.

**Builder focus finding resolved in the parent's isolated phone checks.** The latest `ECOSYSTEM_VERIFICATION.md` addendum records actual keyless browser checks on port 3214: renaming restored focus to **Edit Mira QA**; deletion and cancellation restored **Create a mentor**; manual creation of the Books mentor succeeded and focused **Edit Reading mentor**. Save was reached through the scrollable phone form. I also inspected the explicit save/delete focus restoration in `web/mentoros/ecosystem.js`. These are parent-observed interaction results, not UI tests performed by this critic. They close the reported focus gap and demonstrate manual creation without a provider key; the isolated QA store did not replace the saved live council or planner.

**Largest current blocker: exhausted provider funding. Remaining proof gap: physical microphone capture.** Recording, stopping, transcription, cancellation and transcript review exist in `web/mentoros/ecosystem.js`; prior API round-trip/playback evidence is historical. Presenter-device capture remains unverified, and the newest speech request failed with the external quota error above. The new ecosystem voice journey has no successful current end-to-end verification. The resolved builder focus finding does not change provider availability or establish full keyboard accessibility coverage.

Both live councils converge. Conflict-preservation/resolution is implemented and covered by test source, but a live sustained-disagreement scenario is not demonstrated. Current code also filters unaffordable wellness alternatives more strictly than the retained gym artifact; do not describe that artifact as a fresh execution of every final change.

The documented result of **191 tests passed, one optional live test skipped** precedes an additional isolated billing-message regression described in the latest addendum. No new aggregate count is inferred. Tests were not independently rerun and do not prove current provider availability. `TEST_VERIFICATION.md` retains an older count. Existing planner/DealGuard continuity rests on their documented interaction/recovery evidence and retained implementation, not fresh interaction by this critic. No public deployment, real Slack integration, purchase, booking or external calendar write is established or required for this verdict.
